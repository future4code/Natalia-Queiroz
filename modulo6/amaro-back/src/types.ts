    export type authenticationData = {
        id: string
    }

    export type user = {
        id: string
        name: string
        email: string
        password: string
    }

    export type products = {
        id: string
        name: string
        tags: string[]
    }